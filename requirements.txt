# Core — versions verified from the project environment
pandas==3.0.2
requests==2.33.1
# numpy is installed as a dependency of pandas and will be installed automatically.

# Dashboard & visualisation
# Lower-bound pins prevent known-breaking old versions without over-constraining
# the resolver on the target Linux platform.
streamlit>=1.35.0
folium>=0.17.0
streamlit-folium>=0.20.0
branca>=0.7.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Geospatial
geopandas>=1.0.0
osmnx>=1.9.0
shapely>=2.0.0

# ML
scikit-learn>=1.4.0